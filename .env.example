# Copy this file to `.env` and adjust if your local Postgres differs.
# Requires PostgreSQL >= 15. docker-compose.yml provisions both databases below.

DATABASE_URL="postgresql://clearfeed:clearfeed@localhost:5432/clearfeed"
TEST_DATABASE_URL="postgresql://clearfeed:clearfeed@localhost:5432/clearfeed_test"
